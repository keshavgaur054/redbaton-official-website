export enum TYPES {
    FULL_WIDTH = 'FULL_WIDTH',
    ONE_COLUMN = 'ONE_COLUMN',
    TWO_COLUMN = 'TWO_COLUMN',
    THREE_COLUMN = 'THREE_COLUMN',

}

export const TYPES_LISTS = Object.keys(TYPES);

export enum CATEGORIES {
    WORK = 'WORK',
    RESEARCH = 'RESEARCH',
    SHOWCASE = 'SHOWCASE',
}

export const CATEGORIES_LISTS = Object.keys(CATEGORIES)